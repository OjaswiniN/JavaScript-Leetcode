/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
 
var map = function(arr, fn) {
  var a=[];
    for(i=0;i<arr.length;i++)
    {
        a[i]=fn(arr[i],i);
    }
    return a;
    
};
